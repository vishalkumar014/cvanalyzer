import { Outlet } from 'react-router-dom';

// ==============================|| CommonLayout LAYOUT ||============================== //

const MinimalLayout = () => (
  <Outlet />
);

export default MinimalLayout;
